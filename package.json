{
  "name": "career-profile-platform",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=24",
    "pnpm": ">=12"
  },
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "node scripts/migrate.mjs",
    "test": "pnpm --filter api test && pnpm --filter web test",
    "typecheck": "pnpm --filter api typecheck && pnpm --filter web typecheck",
    "verify": "node scripts/verify.mjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
