# PasswordInput

A standard password field containing:

- password input;
- trailing eye icon button to toggle masked/plain display;
- preserved cursor/value when toggled;
- accessible label (`Show password` / `Hide password`);
- optional password-policy helper and validation error.

Use this component for registration, login, first-password setup, password change, and any future password surface.
